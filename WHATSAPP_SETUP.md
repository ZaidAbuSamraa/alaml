# إعداد إشعارات WhatsApp

تم إضافة ميزة إرسال إشعارات WhatsApp تلقائياً إلى رقم الأدمن عند حدوث أي إشعار في النظام.

## الرقم المستهدف
**+972 59-327-2987**

## أنواع الإشعارات التي يتم إرسالها:

1. **تسجيل دخول موظف** - عندما يقوم موظف بتسجيل الدخول
2. **تسجيل خروج موظف** - عندما يقوم موظف بتسجيل الخروج (مع عدد الساعات)
3. **طلب موارد جديد** - عندما يقوم موظف بإنشاء طلب موارد

## خيارات التكامل:

### الخيار 1: استخدام WhatsApp Business API (موصى به للإنتاج)

1. **التسجيل في WhatsApp Business API:**
   - قم بالتسجيل في [WhatsApp Business Platform](https://business.whatsapp.com/)
   - احصل على API Token

2. **تحديث ملف `.env`:**
   ```env
   WHATSAPP_API_URL=https://graph.facebook.com/v17.0/YOUR_PHONE_NUMBER_ID/messages
   WHATSAPP_API_TOKEN=your_whatsapp_business_api_token
   ```

### الخيار 2: استخدام خدمة طرف ثالث

يمكنك استخدام خدمات مثل:
- **Twilio WhatsApp API**
- **MessageBird**
- **Vonage (Nexmo)**
- **WhatsApp Cloud API**

مثال مع Twilio:
```env
WHATSAPP_API_URL=https://api.twilio.com/2010-04-01/Accounts/YOUR_ACCOUNT_SID/Messages.json
WHATSAPP_API_TOKEN=your_twilio_auth_token
```

### الخيار 3: الوضع الحالي (للتطوير)

حالياً، النظام يطبع الرسائل في console logs فقط. لتفعيل الإرسال الفعلي:

1. قم بإلغاء التعليق على السطور في `.env`:
   ```env
   WHATSAPP_API_URL=your_api_url
   WHATSAPP_API_TOKEN=your_api_token
   ```

2. الكود سيتحقق تلقائياً من وجود هذه المتغيرات ويرسل الرسائل

## كيف يعمل النظام:

1. عند إنشاء إشعار جديد في النظام
2. يتم حفظ الإشعار في قاعدة البيانات
3. **تلقائياً** يتم إرسال رسالة WhatsApp بنفس محتوى الإشعار
4. الرسالة تُرسل إلى الرقم: **+972 59-327-2987**

## الملفات المعدلة:

- `backend/src/whatsapp/whatsapp.service.ts` - خدمة إرسال WhatsApp
- `backend/src/whatsapp/whatsapp.module.ts` - WhatsApp Module
- `backend/src/time-logs/time-logs.service.ts` - إضافة إرسال WhatsApp عند تسجيل الدخول/الخروج
- `backend/src/resource-requests/resource-requests.service.ts` - إضافة إرسال WhatsApp عند طلب موارد

## ملاحظات:

- ✅ الإشعارات تُرسل **تلقائياً** بدون تدخل يدوي
- ✅ الرسائل باللغة العربية
- ✅ يتم تسجيل جميع محاولات الإرسال في console logs
- ⚠️ تحتاج إلى إعداد WhatsApp API للإرسال الفعلي
- 🔒 آمن - لا يتم إرسال بيانات حساسة

## للاختبار:

1. قم بتسجيل دخول موظف من تطبيق الموبايل
2. تحقق من console logs في الـ backend
3. يجب أن ترى رسالة: `WhatsApp notification would be sent to +972593272987: [الرسالة]`

## للإنتاج:

قم بإعداد WhatsApp Business API وتحديث متغيرات البيئة، وسيتم إرسال الرسائل تلقائياً!
